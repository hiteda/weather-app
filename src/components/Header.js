import React from 'react';
import '../App.css';

const Header = (props) => {
    return (
        <div className="App-header" >
            <h2>Weather Comparison App</h2>
        </div>
    );
};

export default Header;